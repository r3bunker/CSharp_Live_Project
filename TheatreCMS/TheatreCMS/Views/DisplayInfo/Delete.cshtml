@using TheatreCMS.Controllers
@model TheatreCMS.Models.DisplayInfo

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>DisplayImage</h4>
    <hr />
    <dl class="dl-horizontal">
      <dt>
        @Html.DisplayNameFor(model => model.Title)
      </dt>

      <dd>
        @Html.DisplayFor(model => model.Title)
      </dd>

      <dt>
        @Html.DisplayNameFor(model => model.TextContent)
      </dt>

      <dd>
        @Html.DisplayFor(model => model.TextContent)
      </dd>

      <dt>
        @Html.DisplayNameFor(model => model.Photo)
      </dt>
      <img src="@Url.Action("DisplayPhoto", "Photo", new { id = Model.PhotoId})" class="thumbnail_size" alt="New Info Image" />

      <!--<dd>-->
      @* model does not exist in the current context?! *@
      <!--@{
  //var img = Model.Image;
  ////displays the image using inline <img tag
  //byte[] thumbBytes = ImageUploadController.ImageThumbnail(img, 200, 200);
  //var thumbBase64 = Convert.ToBase64String(thumbBytes);
  //var imgSrc = String.Format("data:image/png;base64,{0}", thumbBase64);
              }
          </dd>-->

      <dt>
        @Html.DisplayNameFor(model => model.PhotoId)
      </dt>

      <dd>
        @Html.DisplayFor(model => model.PhotoId)
      </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-main" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
